CMAKE FetchContent Example
--------------------------

This example shows the integration of SLICOT into a CMake-enabled project using
CMake's FetchContent mechanism. This requires at least CMake 3.24 to work
properly.

